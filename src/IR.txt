define @main() {

#main:
%n = MOV 5
%sum = MOV 0
%i = MOV 1
jmp 1, #0

#0:
%0 = LE %i, %n
%1 = LNOT %0
jmp %1, #2
%j = MOV 1
jmp 1, #3

#1:
%i = ADD %i, 1
jmp 1, #0

#3:
%2 = LE %j, %n
%3 = LNOT %2
jmp %3, #5
%sum = ADD %sum, %i
jmp 1, #4

#4:
%j = ADD %j, 1
jmp 1, #3

#5:
jmp 1, #1

#2:
ret %sum

#end_main:
}